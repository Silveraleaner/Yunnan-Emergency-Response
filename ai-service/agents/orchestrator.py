from langgraph.graph import StateGraph


def build_workflow() -> any:
    """
    构建 LangGraph 多 Agent 工作流
    
    Returns:
        编译后的 LangGraph workflow 对象
    """
    # TODO: 串联 info_extractor、resource_dispatcher、plan_generator、plan_reviewer 四个 Agent
    pass